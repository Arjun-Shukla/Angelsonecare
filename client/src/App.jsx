/**
 * App — root component. Renders the route table within providers.
 * TODO (implementation): render <AppRoutes /> (providers are applied in main.jsx).
 */

import AppRoutes from './routes/AppRoutes.jsx';

export default function App() {
  return <AppRoutes />;
}
