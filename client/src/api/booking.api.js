/**
 * Booking API calls (maps to /api/bookings).
 *   createBooking, listBookings, getBooking,
 *   acceptBooking, rejectBooking, assignLeader,
 *   updateProgress, generateOtp, verifyOtp
 * TODO (implementation): wire to api (axios).
 */

// import api from './axios.js';

export const createBooking = async (payload) => {};
export const listBookings = async (params) => {};
export const getBooking = async (id) => {};
export const acceptBooking = async (id) => {};
export const rejectBooking = async (id) => {};
export const assignLeader = async (id, leaderId) => {};
export const updateProgress = async (id, status) => {};
export const generateOtp = async (id) => {};
export const verifyOtp = async (id, otp) => {};
