/**
 * Payment API calls (maps to /api/payments).
 *   createOrder, verifyPayment, getPayment
 * TODO (implementation): wire to api (axios) + Razorpay checkout handoff.
 */

// import api from './axios.js';

export const createOrder = async (bookingId) => {};
export const verifyPayment = async (payload) => {};
export const getPayment = async (bookingId) => {};
