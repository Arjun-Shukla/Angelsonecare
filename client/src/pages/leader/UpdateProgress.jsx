/**
 * UpdateProgress page - Leader UpdateProgress - move a booking to In Progress and beyond.
 * TODO (implementation).
 */

export default function UpdateProgress() {
  return <main>{/* UpdateProgress */}</main>;
}
