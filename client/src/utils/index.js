/**
 * Client utility helpers (placeholder).
 *
 * Planned: formatCurrency, formatDate, statusBadgeColor, validators, classNames.
 * TODO (implementation).
 */

export const formatCurrency = (amount) => {};
export const formatDate = (date) => {};
export const classNames = (...classes) => classes.filter(Boolean).join(' ');
