/**
 * Razorpay client initialization.
 *
 * Purpose: Create a single Razorpay SDK instance configured with the key id
 * and secret, reused by `payment.service.js` for order creation and refunds.
 *
 * TODO (implementation):
 *  - export const razorpay = new Razorpay({ key_id, key_secret })
 */

export const razorpay = null;
