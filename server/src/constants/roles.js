/**
 * User role constants used for RBAC across middleware, routes, and models.
 */

export const ROLES = {
  CLIENT: 'CLIENT',
  LEADER: 'LEADER',
  ADMIN: 'ADMIN',
};

export const ROLE_VALUES = Object.values(ROLES);
