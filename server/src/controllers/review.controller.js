/**
 * Review controller.
 *
 * Endpoints:
 *   createReview   -> client submits review after completion
 *   listReviews    -> public list of approved reviews (for website)
 *   listMyReviews  -> client's own reviews
 *   approveReview  -> admin approves for public display
 *   deleteReview   -> admin removes a review
 *
 * Delegates to `review.service.js`; emits review:new.
 * TODO (implementation).
 */

export const createReview = async (req, res) => {};
export const listReviews = async (req, res) => {};
export const listMyReviews = async (req, res) => {};
export const approveReview = async (req, res) => {};
export const deleteReview = async (req, res) => {};
