/**
 * User controller (admin-scoped).
 *
 * Endpoints:
 *   listUsers     -> list/filter users by role (admin)
 *   getUser       -> get a single user
 *   createLeader  -> create/promote a Leader account
 *   updateUser    -> update profile / activate-deactivate
 *   deleteUser    -> remove a user
 *
 * TODO (implementation): wire to user logic + apiResponse.
 */

export const listUsers = async (req, res) => {};
export const getUser = async (req, res) => {};
export const createLeader = async (req, res) => {};
export const updateUser = async (req, res) => {};
export const deleteUser = async (req, res) => {};
