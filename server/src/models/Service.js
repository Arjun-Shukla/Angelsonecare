/**
 * Service model — the catalog of healthcare services offered.
 *
 * Planned fields:
 *   name        : String, required
 *   description : String
 *   price       : Number, required
 *   category    : String
 *   image       : String (URL)
 *   isActive    : Boolean, default true
 *   timestamps  : createdAt / updatedAt
 *
 * Relationships:
 *   - 1—N Bookings (Booking.service)
 *
 * TODO (implementation): define mongoose schema, export model.
 */

export default null;
