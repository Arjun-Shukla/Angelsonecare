/**
 * Auth service — business logic for authentication.
 *
 * Responsibilities:
 *   - findOrCreateGoogleUser(profile)
 *   - issueTokens(user) -> { accessToken, refreshToken }
 *   - rotateAccessToken(refreshToken)
 *
 * TODO (implementation): uses utils/jwt, models/User.
 */

export const findOrCreateGoogleUser = async (profile) => {};
export const issueTokens = (user) => {};
export const rotateAccessToken = (refreshToken) => {};
