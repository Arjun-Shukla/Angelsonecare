/**
 * Dashboard socket handlers (admin live dashboard).
 *
 * Purpose: Admin sockets join `role:admin` to receive live dashboard updates
 * (new bookings, tickets, reviews, revenue changes).
 *
 * TODO (implementation): any admin-specific subscribe/refresh listeners.
 */

export const registerDashboardHandlers = (io, socket) => {};
