/**
 * Auth request validation schemas (placeholder).
 * Define schemas for refresh/logout payloads as needed.
 * TODO (implementation): export validation schemas.
 */

export const refreshSchema = null;
