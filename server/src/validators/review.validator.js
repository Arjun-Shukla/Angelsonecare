/**
 * Review request validation schemas (placeholder).
 *
 * Planned: createReviewSchema (bookingId, rating 1-5, comment).
 * TODO (implementation): export validation schemas.
 */

export const createReviewSchema = null;
